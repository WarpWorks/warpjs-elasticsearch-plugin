{{#if page.isNext}}
  {{#if page.disabled}}
    <span><span aria-hidden="true">
  {{else}}
    <a href="{{page._links.self.href}}">
  {{/if}}
      &gt;
  {{#if page.disabled}}
    </span></span>
  {{else}}
    </a>
  {{/if}}
{{/if}}
